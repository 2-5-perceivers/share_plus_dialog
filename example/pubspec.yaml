name: example
description: Example

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  share_plus_dialog:
    path: ../
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
